#include <stdio.h>


/**
 * main -> this is the function
 * @argc: argc parameter
 * @argv: argv parameter
 * Return: (0) for success
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
printf("%s\n", *argv);
return (0);
}
