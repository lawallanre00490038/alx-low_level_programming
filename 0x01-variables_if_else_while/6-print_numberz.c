#include <stdio.h>

/**
 *main - Entry point
 *
 *Description: 'this is the print of alphabet in lower case'
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
int num;
for (num = 48; num <= 57; num++)
{
putchar(num);
}
putchar(10);
return (0);
}
