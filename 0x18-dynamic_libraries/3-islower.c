#include "main.h"


/**
* _islower - returns true is all letters are lower case
* @c: the character to check
* Description: Takes and argumant and determines if its lower case
* Return: 1 if lower case
*/

int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
