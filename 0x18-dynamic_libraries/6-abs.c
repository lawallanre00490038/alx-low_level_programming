#include <stdio.h>
#include "main.h"

/**
 * _abs - return the absolute value
 * @n: the integer parameter
 * Return: the absolute value is returned here
 */

int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
